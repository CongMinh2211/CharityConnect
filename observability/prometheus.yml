global:
  scrape_interval: 10s

scrape_configs:
  - job_name: identity
    static_configs: [{ targets: ["identity-service:3001"] }]
  - job_name: campaign
    static_configs: [{ targets: ["campaign-service:3002"] }]
  - job_name: donation
    static_configs: [{ targets: ["donation-service:8000"] }]

